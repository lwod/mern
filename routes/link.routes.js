const Link = require('../models/Link')

const {Router} = require('express')
const router = Router()

router.post(
	'/generate',
	async (req,res)=>{
	
})

router.get(
	'/',
	async (req,res)=>{
	
})

router(
	'/:id',
	async (req,res)=>{
	
})

module.exports = router()
